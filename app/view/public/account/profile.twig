{% extends "public/base.twig" %}

{% block content %}

<section id="last">
    <div class="container">
        <div class="row">
            <div class="col-lg-8 offset-lg-2 text-center">
                <h2 class="margin-top-0 wow fadeIn">{{ lang.GREETING }} {{session.username}}</h2>
                <hr class="primary">
                <p>{{ lang.WELCOME_BACK }}</p>
            </div>

            <div class="row">
            	<div class="col-lg-4 offset-lg-2">
            		<a href="/produkty/pridat" class="btn btn-primary">{{ lang.PRODUCT_NEW }}</a>
            	</div>
            </div>
            
        </div>
    </div>
</section>

{% endblock %}